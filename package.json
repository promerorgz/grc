{
  "name": "dope-grc",
  "version": "0.1.0",
  "description": "quick generate react components",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pmin": "git add . && git commit -m 'minor version bump' && git push && npm version minor && npm publish",
    "pmax": "git add . && git commit -m 'major version bump' && git push && npm version major && npm publish"
  },
  "author": "pr",
  "license": "ISC"
}
